---
import Layout from "../layouts/layout.astro";
import {socials} from "../data";
---

<Layout	title="main">

	<div class="px-4 ">
	<div class=" text-2xl flex justify-between pt-16 ">
		shivam
	</div>
	<a  href="/cinema" class="flex flex-col text-4xl text-white text-opacity-60 hover:text-opacity-100 fixed bottom-8 left-8 max-sm:text-4xl   lg:pt-6 xl:pt-2 2xl:pt-2  ">🎥</a>
		<div class="flex max-sm:text-2xl max-sm:flex-col   h-auto w-auto  p-1 pt-4 lg:pt-6 xl:pt-4 2xl:pt-6 gap-3">
		  {socials?.map((x) => (
			  <a
				href={x?.link}
				target="_blank"
class=" hover:text-opacity-60 max-sm:underline  text-blue-700 text-2xl"
			  >{x.title}</a>
		  ))}
		</div>
	  </div>
</Layout>